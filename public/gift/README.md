# Подарок (чек-лист)

`checklist.pdf` здесь — **заглушка**. Замени её на реальный чек-лист «Где сайт теряет заявки»
(тот же путь и имя, либо задай свой `GIFT_URL` в .env).

Файл отдаётся по `APP_BASE_URL/gift/checklist.pdf`.
